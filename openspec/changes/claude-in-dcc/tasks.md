# Claude in DCC — tasks

Appetite: **large**. Order: ledger → chat → actions → control center →
what moves to the chat → lifecycle. Every stage leaves the system whole.
Branches: `project/claude-in-dcc`, one `task/…` branch per stage.

## 0. Groundwork

- [x] 0.1 Requirement (`docs/claude-in-dcc.md`), current state, plan with prior-art research and eight screens; screens reviewed and reduced to three (one chat over every screen, one control center, the cost line as a component)
- [x] 0.2 Navigable mock (`claude-in-dcc-mock-v2.html`) approved: floating launcher bottom-left, floating / pinned chat, per-topic conversations, proposal / declared-cost / refusal cards, suggestions per screen

## 1. The ledger · `task/claude-ledger`

- [x] 1.1 `@dcc/db`: `claude_call` schema + migration `0035_claude.sql` (RLS, indexes, `source_ref` unique, `meta`); append-only triggers in `sql/guards.sql`; the cached spend column on `client_budget` dropped
- [x] 1.2 `@dcc/db`: `recordClaudeCall()` — the only writer — with input validation; `claude.call` thin event type; `claude_call` link rel
- [x] 1.3 `@dcc/core`: `runClaudeRaw` takes a required `ledger` context, calls `route()` for every call, records the row (outcome on error/timeout/stop/refused too); `runClaudeJson` and every caller updated; cache token buckets read from the CLI result; a call over the capability's input cap is refused before it runs
- [x] 1.4 `@dcc/core`: policy file gains `version` + `prices`; capabilities `chat`, `chat_code_read`, `conversation_summary`, `usage_insights`, `interactive_session` (two more stayed until stage 5 removed their callers); `brief` / `matching` / `narrative` / `review` removed; the routing audit event and its recorder, the timeline summariser that never summarised, and the `/workitems/:id/route` endpoint deleted; `routing.prove.ts` updated (15 checks green)
- [x] 1.5 `@dcc/core`: onboarding session recorded as ledger slices (stage complete, process exit, resume, cancel); run view's cost built from the ledger + the live unrecorded delta (shown as "not yet recorded"); the assistant's calls are ledger rows too, so its totals are no longer kept on the run (the assistant itself goes in stage 2)
- [x] 1.6 `@dcc/core`: hook sessions (`recordSession`) also write a ledger row; `requirementCostSummary` / `requirementCostDetail` / budgets / dashboard / clients read from the ledger, month to date
- [x] 1.7 `@dcc/db`: `dev:backfill-ledger` — historical session events and onboarding sessions into the ledger, idempotent by `source_ref`; `dev:prove` gains five checks for the ledger (RLS wall, append-only, the thin timeline event with no cost) — 14/14 green
- [x] 1.8 API: `GET /claude/overview`, `GET /claude/calls`, `GET /claude/calls/:id`, `GET /workitems/:id/calls`; the routing endpoint and the `dcc.mjs route` subcommand deleted (routing is inside every call)
- [x] 1.9 Web: `CostLine`, `CallsTable`, `claude/labels.ts` (one wording for model, effort, capability, outcome — the onboarding labels re-export it); "קלוד" in the sidebar; `ClaudeCenter` screen with סקירה and קריאות; `Record.tsx`'s cost detail, the onboarding `CostPanel` moved to the shared components
- [x] 1.10 Migration applied to the local database with the API stopped; typecheck (`tsc -b` + `apps/web`), `audit:stale`, `dev:prove` green; the control center, the requirement screen's cost detail, budgets and the dashboard checked live. Found on the way: a database rebuilt by `dev:setup` could not be migrated incrementally (`dev:setup` never recorded what it applied, and an old file cannot re-run once a later one dropped its column) — `dev:setup` now records, and `dev:adopt <file>` marks what an existing database already has
- [x] 1.11 The earlier cost-tracking OpenSpec change deleted (the ledger replaces it); its names on the retired list

## 2. The chat · `task/claude-chat`

- [x] 2.1 `@dcc/db`: `conversation`, `conversation_message` (in `0035_claude.sql`), RLS, indexes
- [x] 2.2 `@dcc/core` `chat/`: topic keys (one conversation per client × topic × person), prompt assembly (the screen's glossary + facts, sent only when their hash changed), lean CLI call with `--session-id` / `--resume` and no tools, cumulative-cost delta per session, a fresh session when the old one is gone, the input cap through the ledger, roll-over on size or age with a recorded `conversation_summary` call. Found live: on a resumed session the CLI reports only the fresh tokens as input (3) and the history through the cache buckets (5,655 written) — the conversation's size is their sum
- [x] 2.3 `@dcc/core` `glossary/`: registries for requirement, task, pull request, onboarding, control center, dashboard, budgets (one wording for the `?` hint, the chat and the docs); step-zero matcher — screen question → glossary `about`, fact templates (next step, cost, owner, status, gaps, blocker) before glossary terms, glossary term when the question is short and about it
- [x] 2.4 `@dcc/core`: "did this help" — marked by the person, or the same question again within a minute (`helpful_source = reasked`); `[אין לי את זה במסך]` marker → `unanswered` on the ledger row
- [x] 2.5 API: `POST /claude/chat/open`, `POST /claude/chat/ask`, `POST /claude/messages/:id/helpful`, `GET /claude/conversations`, `GET /claude/conversations/:id`, `GET /claude/glossary/:screen`; the `app` topic lives on the client named "DCC Internal" (else `.dcc.json`'s, else the first client)
- [x] 2.6 Web: `useClaudeContext` (a store the dock reads; `liveFacts` read at the moment of asking), `ClaudeChat` (launcher bottom-left, `Ctrl K`, floating or pinned beside the screen — remembered, unavailable under 1100 px; topic bar; chips from the screen; the cost line and עזר / לא עזר on every answer), `GlossaryHint` (`?` on the requirement's run buttons, the pull request's review button, the control center's tiles); mounted once in `App.tsx`; context registered on requirement, task, pull request, onboarding, control center, dashboard, budgets
- [x] 2.7 Web: שיחות tab, grouped by day with cost and calls; a row opens that conversation in the dock; `#/claude/conversations/<id>` does the same
- [x] 2.8 The onboarding reading aid deleted — its core module, its panel, its API routes, its per-run store file, its capability and its OpenSpec change; its names on the retired list. `digestTranscript` and the changed-files summary now feed the `run:<id>` topic's facts (`onboardingChatFacts`); "send to session" stays in core for the stage-3 action registry; the event it wrote is renamed `onboarding.session.instructed`
- [x] 2.9 Verified live against the API and in the dock: "מה הכפתור פרק למשימות עושה?" and "מה המסך הזה מציג?" answered from the glossary at $0.000 (source = system); three model questions answered by Haiku 4.5 at low effort — three ledger rows with tokens, cache buckets, duration and the policy rule (`default haiku for chat`, v2), $0.023 in all; the conversation listed with 6 messages and 3 calls. Note for the next tester: Hebrew sent through `curl` from Git Bash arrives garbled (console code page) — use a UTF-8 payload file or the browser

## 3. Actions · `task/claude-actions`

- [x] 3.1 `@dcc/core` `actions/`: the registry (`ActionDef` — title, consequence, topic, declared parameters, describe / allowed / estimate / preview / run); `assess`, `breakdown`, `implement`, `approve_task`, `send_to_session` registered; the four screen buttons (`/workitems/:id/assess`, `/breakdown`, `/tasks/:id/implement`, `/tasks/:id/approve`) go through `runAction(…, "button")`, so the button and the chat's proposal share one gate (`allowed`) and one run. Not registered, deliberately: `update_branch` — the pull-request action screens are still disabled (pull-request-center 6.1 / 11.8), and an action joins the registry only once its button exists; `send_gap_question` — there is no such host action in DCC (the letter to the client moves into the chat in stage 5)
- [x] 3.2 Chat: `<action key>{json}</action>` parsed; parameters narrowed to the ones the action declares; validated (exists on this topic, `allowed` for this person) → `proposal` message (title, "what is about to happen", estimate from the policy, status), else a `refusal` message with the reason; `POST /claude/proposals/:id/run` runs through the registry in the approver's name with trigger `chat` (owner check; `ActionRefused` → 409 with the Hebrew reason; status proposed → running → done / failed); `/cancel`; `/preview` opens the action's own "what will be sent" prompt. Found live: a conversation whose model session began under an earlier system prompt kept answering "I cannot run actions" from its own history — the baseline now keeps a hash of the rules the session was started under, and a changed hash is a roll-over reason (summary call recorded as `כללי הצ'אט התעדכנו`); the prompt file is rewritten whenever its text changed; a session that has to start over receives the full opening (topic, context, question), not only the delta
- [x] 3.3 Expensive question: `<needs_code>` → `declared_cost` message (reason, model / effort from the policy, $0.05–0.40 range, read-only access) → `POST /claude/messages/:id/run-code` runs `chat_code_read` with `Read,Grep,Glob` on the requirement's local copy (or a run's isolated copy), `parent_call_id` = the asking call, the answer inserted as a model message (`from: code`); `/run-code/cancel`. Refusals in words: no linked repository, no local copy yet, a topic without code. The requirement screen now tells the chat which repositories are linked ("מאגרים מקושרים"), so the model knows whether there is code to read
- [x] 3.4 Web: `ProposalCard` — proposal (אשר והרץ / הצג את מה שיישלח → `PromptPreviewModal` / בטל; a run that only starts here says so and points to the screen; "already running" when the screen had one going), declared-cost card (הרץ / לא עכשיו; done → "the answer is below, its cost recorded under this question"), refusal card; a code answer is labelled as read from the repository with approval. `PromptPreviewModal` shows the English prompt with a note when an action has no Hebrew rendering (the readiness check) instead of an empty Hebrew tab
- [x] 3.5 Verified live: "תריץ בחינת בשלות על הדרישה הזו, עם דגש על תאריכי יעד" → Haiku answered "אני מציע…" ($0.007) with a proposal card (`assess`, `customEmphasis` filled by the model); preview showed the real assess prompt; approved → the readiness check ran through the registry, `gap_detection` row with trigger `chat` in the approver's name, $0.072, the requirement moved to shaping with 4 gaps; a second proposal cancelled from the dock ("לא בוצע. שום דבר לא נרשם"); cancelling twice refused ("ההצעה כבר לא ממתינה"). "איזה קובץ בקוד של המאגר כותב את הרשומות ליומן…" on a requirement with `delivery-control-center` linked → declared-cost card → approved → Sonnet 5 read the code (4 turns, 12 s, $0.045) and answered correctly (`recordCall` in `packages/core/src/ai-assist.ts`, best-effort on failure); its ledger row carries `parent_call_id` of the asking call, which is marked `unanswered`. Found on the way: the ledger took the first key of the CLI's `modelUsage` as the model used, so a Sonnet call showed as Haiku (the CLI lists its own small helper calls too) — the model that cost the most is now the one recorded. Not exercised live: `implement` / `approve_task` / `send_to_session` through a proposal (their `allowed` gates and runs are the same code the buttons use); a `<needs_code>` on a requirement without a repository is answered with the refusal sentence, and the model tends to propose the readiness check instead once it knows there is no code

## 4. Insights and policy · `task/claude-insights-policy`

- [x] 4.1 `@dcc/core` `insights.ts`: clustering is one SQL query (person's questions, normalised in SQL the way the chat's "asked again" check normalises, grouped by client × screen × question, two repeats or more); `claude_insight` (migration `0036_claude_insights.sql`, RLS, unique per cluster) keeps the finding, the recommendation, the analysing call and what was done; "נתח שאלות" is the named action — one `usage_insights` call (trigger `insights`, screen `claude`, on the filtered client or the internal one) for the clusters above `insightsMinRepeats` that have no finding yet or grew since, answered as JSON and upserted; "פתח משימת שיפור" creates a requirement on the internal client in the person's name with the finding and recommendation as its first `note.added`, idempotent (a second press returns the same task); "לא רלוונטי" keeps the count and drops the row. Lists: unanswered (ledger rows with the marker), unhelpful (answers marked by a person or re-asked, with the question they answered), failed (error / timeout / refused), escalated — all slices of the ledger and the conversations. `dev:prove` gains two checks for the wall on conclusions (16/16)
- [x] 4.2 `@dcc/core` `policy-admin.ts`: `updatePolicy` validates (tiers, prices, capability values, chat thresholds, guardrails; an unknown capability is refused — the editor changes values, never the list), deep-merges over the file, bumps `version` through `savePolicy`, records `policy.changed` (from → to version, the changed paths with old and new values) on the internal client in the person's name; `policyView` returns the policy, the last change with who made it, and every client's retention period; `setClientRetention` writes `client.chat_retention_days` and records the same event on that client; the chat's `retain_until` now uses the client's period when one is set. Found on the way: `savePolicy` rewrote the file in `JSON.stringify` layout (a one-value change became a 100-line diff) — it now writes the file's own layout (sections indented, entries on one line), proven by `routing.prove.ts` (16/16)
- [x] 4.3 API: `GET /claude/insights`, `POST /claude/insights/analyse`, `POST /claude/insights/:id/task`, `POST /claude/insights/:id/dismiss`, `GET /claude/policy`, `PUT /claude/policy`, `PUT /clients/:id/claude-retention`; `PolicyError` → 409 with the Hebrew reason; `policy.changed` payload registered; the audit trail names the event
- [x] 4.4 Web: מסקנות — the repeated questions with count, threshold, finding and recommendation, the analyse button carrying its estimate and the cost note, "פתח משימת שיפור" / "לא רלוונטי", the unanswered and unhelpful lists (a row opens the conversation), failed and escalated in the rail, "what to do with a conclusion"; מדיניות ושמירה — per capability the tier (with its model), effort, per-call cap and input cap with plain-Hebrew explanations, the tiers' models and caps, the chat thresholds with what each one does, retention per client, the price list (read-only), the version and the last change (who, when, what), save / undo; glossary entries for מסקנות, נתח שאלות, פתח משימת שיפור, סף החזרות
- [x] 4.5 Verified live: the clustering found "תריץ בחינת בשלות…" asked 5 times on the requirement screen (threshold 5) → "נתח שאלות" made one Sonnet call ($0.007, 22 s) and worded a finding ("people did not know the check can be run with a focus…") and a recommendation (a focus choice next to the button); "פתח משימת שיפור" created `שיפור מסך דרישה: "…"` on DCC Internal with the note, a second press returned the same task (200, `created: false`), the row shows "משימת שיפור נפתחה". Policy: `PUT` chat effort low → medium gave version 3 with `capabilities.chat.effort` recorded; the next chat question's ledger row shows Haiku, effort **medium**, policy version **3**; the revert gave version 4; an unknown capability was refused (409); the policy view shows the last change with the person's name. Retention: טרייד set to 30 days and back to the default, both recorded as `policy.changed` on that client; 0 days refused. Both tabs checked in the browser. Found on the way, in the "נדרש מודל חזק יותר" list: every readiness check counted as "model overridden by user (sonnet)" — the assess template names its tier by alias and `route()` compared the alias with the resolved model id; a choice is now resolved through the tiers first, so the same tier as the policy's is not an override (two checks added to `routing.prove.ts`, 18/18)

## 5. What moves to the chat · `task/claude-chat-topics`

- [x] 5.1 Chips in the requirement's conversation: "נסח מכתב ללקוח עם השאלות הפתוחות" (only while there are open gaps) and "המלצות לייעול: מה היה כדאי לעשות אחרת?" (once there are tasks or the requirement is done); the chat's rules gained a LETTER paragraph (the whole message from the open gaps in the facts, numbered, plain business Hebrew, longer than the usual limit, "DCC never sends it — copy it") and a RECOMMENDATIONS paragraph (three to five points tied to the facts, never generic). The requirement's facts no longer hand the model a client id (a UUID it could only misuse)
- [x] 5.2 Deleted: the end-of-requirement recommendations run (its result type, prompt builder, run kind, own run view, kick-off and polling routes, web calls and modal), the composed letter to the requester (its composer, history reader, event type and payload schema, routes, web calls, the three modals and the header links on the requirement screen), their two capabilities in the policy and the `Capability` type, their labels and glossary entries, the back-fill's map entries (an old run keeps its old name as its capability — history is not rewritten); the two OpenSpec changes that introduced them removed with `git rm`; the prompt template the letter used is deleted by migration `0037_retire_letter_prompt.sql`
- [x] 5.3 Sweep (`grep -rIn --exclude-dir=node_modules,dist,.git,.pgdata` for 24 identifiers, positive control on a live name): zero hits outside `docs/history/`, applied migrations and git's own logs; prose that named the old design reworded in `decision-history`, `requirement-types`, `docs/sessions/gaps-readiness-session.md` and this change's proposal, design (§7 now states what is a button and what is the chat, no mapping table) and tasks; the identifiers added to `scripts/audit-stale.mjs`; `audit:stale` green; typecheck (`tsc -b` + `apps/web`) green

## 6. Lifecycle and measurement · `task/claude-lifecycle`

- [x] 6.1 Roll-over on tokens / age (and on changed rules, from stage 3) with a recorded summary — proven live: the threshold lowered to 2,000 tokens through the policy editor (version 5) → the next question on a ~7,000-token conversation rolled over: a `conversation_summary` row (trigger `rollover`, Haiku, $0.011, policy version 5), the old conversation `rolled` with `continues_as`, the new one `continued_from` it with the summary as its first note; threshold restored (version 6). The dock shows the continuation as a divider line with the summary on hover; שיחות shows "התגלגלה להמשך" on the old row
- [x] 6.2 Retention: `archiveExpiredConversations` (every message → one fixed sentence, no payload, no note; `status = archived`; session id, context hash and the CLI session folder gone; active and rolled alike; idempotent), scheduled inside the API process once after start and then daily (`scheduleRetention`, timers unref'd), and on demand through `POST /claude/retention/run`; the per-client period from stage 4 stamps `retain_until` at every message. Proven by `npm run -w @dcc/core prove:retention` against a scratch database (7/7: counts, blanking, state, folder deleted, a rolled one archived too, a fresh one untouched, second pass empty; the script refuses to run without `DCC_PGLITE_DIR`); live: the on-demand pass answered `{archived: 0, messages: 0}` — nothing is past its period yet
- [x] 6.3 The measurements in the control center's overview ("מה מכוונים לפיו"): cost per question, tokens per turn, roll-overs with their summaries' cost, conversations removed by retention, per screen the share answered without a model and the share marked unhelpful, per capability the escalation share with the 30% rule. Read live this month: $0.004 per question, 5,925 tokens per turn, 5 roll-overs ($0.026), 0 archived, the requirement screen 29 questions · 17% without a model · 3% unhelpful, cache share 57%; the one "100% escalated" readiness check is the row recorded before the tier-alias fix of stage 4 — later rows count as defaults
- [ ] 6.4 Project branch current with `master`; typecheck, `audit:stale`, `dev:prove` green; pull request `project/claude-in-dcc → master`
