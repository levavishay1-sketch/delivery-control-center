/**
 * What each chat-registered screen is for — the answer to "מה המסך הזה מציג?".
 * A screen appears here when it calls `useClaudeContext`; the concepts it can
 * answer about are the ones whose `screens` list names it (see `concepts/`).
 * `scripts/audit-stale.mjs` checks every registered screen has a line here.
 */
export const SCREEN_ABOUT: Record<string, string> = {
  requirement:
    "מסך של דרישה אחת: מה ביקשו, באיזה שלב זה, מה עדיין פתוח (פערים וחוסמים), אילו משימות נגזרו ממנה, וכמה עלה ה-AI עליה עד עכשיו. מכאן מריצים את השלבים: בחינת בשלות, פירוק למשימות, ואישור המשימות ב-TFS.",
  task:
    "מסך של משימה אחת מתוך דרישה: מה צריך לעשות, האם היא אושרה, מה קלוד ביצע בה, ומה קרה בבדיקות. מכאן מאשרים משימה, מריצים את הפיתוח בעותק מבודד, ודוחפים את התוצאה.",
  pull_request:
    "מסך של בקשת מיזוג אחת: מאיזה ענף לאיזה, האם היא עדיין מבוססת על קוד עדכני, מה חסר לפני מיזוג (סקירה, בדיקות, התנגשויות), ומה קרה לה לאורך הזמן. הפעולות עצמן — עדכון, סקירה, מיזוג — נעשות בשם שלכם באתר המארח.",
  onboarding:
    "הטמעת מאגר: הכנה של המאגר לעבודה עם Claude Code, בארבעה שלבים סביב סשן חי אחד — הכנת עותק מבודד, ההטמעה עצמה (/init של Claude Code, בטרמינל), סקירה של כל קובץ שנוצר, ומסירה ל-git כבקשת מיזוג.",
  claude:
    "מרכז הבקרה של קלוד: כל קריאה, כל שיחה, כל הכסף, המדיניות והמסקנות — במקום אחד. כל מספר על קלוד במסך אחר הוא חתך ממה שכאן.",
  dashboard:
    "לוח הבקרה: מה פתוח, מה חסום, כמה ה-AI עלה החודש, הדרישות האחרונות וההתראות. מכאן מוסיפים דרישה חדשה ומגיעים לכל מסך אחר.",
  budgets:
    "תקציבים: עלות ה-AI החודשית של כל לקוח מול התקציב שהוגדר לו. הכסף שמוצג כאן הוא סכום הקריאות ביומן הקריאות של קלוד מתחילת החודש.",
};
